# SimCluster
Ce projet consiste à développer une application distribuée en C# (.NET) qui simule un système de traitement de tâches dans un cluster. L’objectif est de reproduire le fonctionnement d’un scheduler distribué comme dans un vrai système cloud.
L’application sera composée de plusieurs services conteneurisés (Docker), déployés et orchestrés sur un cluster Kubernetes.
